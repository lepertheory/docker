#!/usr/bin/env bash

set -o errexit
set -o pipefail
set -o nounset

./configure --prefix=/tools --without-guile
make
# Test times out sporadically. Not needed during bootstrap, so skipping.
#make check
make install
