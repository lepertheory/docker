#!/usr/bin/env bash

# SBUS: 0.8

set -o errexit
set -o pipefail
set -o nounset

./configure --prefix=/tools                \
            --without-python               \
            --disable-makeinstall-chown    \
            --without-systemdsystemunitdir \
            --without-ncurses              \
            PKG_CONFIG=''
make
make install
