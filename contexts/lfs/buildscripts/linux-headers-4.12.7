#!/usr/bin/env bash

# SBUS: 0.1

set -o errexit
set -o pipefail
set -o nounset

make mrproper

make INSTALL_HDR_PATH=dest headers_install
find \( -name .install -o -name ..install.cmd \) -delete
cp --recursive --verbose dest/include/* /usr/include
