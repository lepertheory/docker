#!/usr/bin/env bash

# SBUS: 0

set -o errexit
set -o errtrace
set -o pipefail
set -o nounset

# FIXME: This script is only necessary because `../../lfs-bootstrap/buildscripts/build` line 44 requires it.
"$@"
